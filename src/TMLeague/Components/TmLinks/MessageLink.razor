@if (string.IsNullOrEmpty(PlayerName))
{
    @PlayerName
}
else
{
    <a href="@ThroneMasterUriProvider.GetMessageUri(PlayerName)" class="text-inherit" target="_blank">@PlayerName</a>
}

@code {
    [Parameter]
    public string? PlayerName { get; set; }
}