@if (string.IsNullOrEmpty(PlayerName))
{
    @PlayerName
}
else
{
    <a href="@ThroneMasterUriProvider.GetPlayerUri(PlayerName)" class="text-inherit" target="_blank">@PlayerName</a>
}

@code {
    [Parameter]
    public string? PlayerName { get; set; }
}